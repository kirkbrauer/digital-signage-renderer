import { Component } from 'react';
import { RenderWidgetProps } from '../index';

export class WeatherWidget extends Component<RenderWidgetProps> {
  render() {
    // TODO: Add weather widget render code
    return null;
  }
}
