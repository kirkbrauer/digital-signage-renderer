import { Component } from 'react';
import { RenderWidgetProps } from '../index';

export class TextBoxWidget extends Component<RenderWidgetProps> {
  render() {
    // TODO: Add text box widget render code
    return null;
  }
}
