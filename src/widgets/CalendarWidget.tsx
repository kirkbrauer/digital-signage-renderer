import { Component } from 'react';
import { RenderWidgetProps } from '../index';

export class CalendarWidget extends Component<RenderWidgetProps> {
  render() {
    // TODO: Add calendar widget render code
    return null;
  }
}
